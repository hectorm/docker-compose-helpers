#!/bin/sh

set -eu
export LC_ALL=C

if docker compose version >/dev/null 2>&1; then
	COMPOSE='docker compose'
else
	COMPOSE='docker-compose'
fi

if [ -t 1 ]; then
	${COMPOSE:?} up -d "${@}"
	exec ${COMPOSE:?} logs -f
else
	exec ${COMPOSE:?} up "${@}"
fi
